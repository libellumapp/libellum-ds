{
	"private": true,
	"workspaces": [
		"packages/docs",
		"packages/eslint-config",
		"packages/react",
		"packages/tokens",
		"packages/ts-config"
	],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
	"devDependencies": {
		"turbo": "^1.7.0"
	}
}
